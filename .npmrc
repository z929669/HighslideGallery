# npm configuration for HighslideGallery
# Run in WSL: npm install --save-dev (uses this config)

# Prefer offline mode for reproducibility (caches packages)
prefer-offline=true

# Use exact versions (no ^ or ~ ranges)
save-exact=false

# Audit for security issues on install
audit-level=moderate

# Registry (default is npm; can override for corporate proxies)
registry=https://registry.npmjs.org/
